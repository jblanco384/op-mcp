{
  "_comment": "Field Group Support: You can now use '@GroupPrefix' to include all fields from a field group. For example, '@OPSS-Ctl' will include all fields with the OPSS-Ctl: prefix. Mix individual fields and groups as needed.",
  "global_settings": {
    "output_format": "json",
    "output_format_description": "Global output format for upsert/delete/query_* tool responses. Options: 'text' (human-readable) or 'json' (structured, machine-readable for agents). Note: execute_openpages_query tool has its own 'format' parameter (table/json/list) for query-specific formatting.",
    "namespace": "openpages",
    "namespace_description": "Global namespace prefix for generic tools (e.g., delete_object becomes openpages_delete_object)",
    "tool_exposure_mode": "ontology_based",
    "tool_exposure_mode_description": "Controls which tools are exposed. Options: 'all' (both ontology_based and type_based), 'ontology_based' (only ontology based generic tools like upsert_object), 'type_based' (only type-specific tools like upsert_control)"
  },
  "object_types": [
    {
      "type_id": "SOXControl",
      "tool_prefix": "control",
      "display_name": "Control",
      "path_prefix": "Controls",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a SOX control in OpenPages. Automatically determines whether to insert a new control or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve SOX controls from OpenPages. Supports filtering by name, owner, and custom field filters. Returns control details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-Ctl"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_control tool. Using '@OPSS-Ctl' includes all fields from the OPSS-Ctl field group. You can also mix groups with individual fields, e.g., ['@OPSS-Ctl', 'SomeOtherField']"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Ctl:Status",
          "OPSS-Ctl:Control Type",
          "OPSS-Ctl:Frequency",
          "OPSS-Ctl:Owner"
        ],
        "_comment": "USED BY: Type-based query_controls tool ONLY. Individual fields listed here. Could also use '@OPSS-Ctl' to include all OPSS-Ctl fields"
      }
    },
    {
      "type_id": "SOXIssue",
      "tool_prefix": "issue",
      "display_name": "Issue",
      "path_prefix": "Issue",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update an issue in OpenPages. Automatically determines whether to insert a new issue or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve issues from OpenPages. Supports filtering by name, owner, and custom field filters. Returns issue details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": false,
        "fields": [
          "@OPSS-Iss"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_issue tool. Using '@OPSS-Iss' to include all fields from the OPSS-Iss field group"
      },
      "type_based_query_filters": {
        "fields": [
          "@OPSS-Iss"
        ],
        "_comment": "USED BY: Type-based query_issues tool ONLY. Using field group for filters as well"
      }
    },
    {
      "type_id": "SOXRisk",
      "tool_prefix": "risk",
      "display_name": "Risk",
      "path_prefix": "Risk",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a risk in OpenPages. Automatically determines whether to insert a new risk or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve risks from OpenPages. Supports filtering by name, owner, and custom field filters. Returns risk details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": false,
        "fields": [
          "OPSS-Rsk:Status",
          "OPSS-Rsk:RiskLevel",
          "OPSS-Rsk:Owner"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_risk tool. Individual fields listed. Could use '@OPSS-Rsk' to include all OPSS-Rsk fields, or mix: ['@OPSS-Rsk', 'OtherGroup:Field']"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Rsk:Status",
          "OPSS-Rsk:RiskLevel",
          "OPSS-Rsk:Owner"
        ],
        "_comment": "USED BY: Type-based query_risks tool ONLY. Individual fields for query filters"
      }
    },
    {
      "type_id": "Register",
      "tool_prefix": "usecase",
      "display_name": "Use Case",
      "path_prefix": "Registers",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a usecase in OpenPages. Automatically determines whether to insert a new usecase or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve usecase from OpenPages. Supports filtering by name, owner, and custom field filters. Returns usecase details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": false,
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_usecase tool"
      },
      "type_based_query_filters": {
        "_comment": "USED BY: Type-based query_usecase tool ONLY. No specific filters configured"
      }
    },
    {
      "type_id": "Vendor",
      "tool_prefix": "vendor",
      "display_name": "Vendor",
      "path_prefix": "Vendors",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a vendor in OpenPages. Automatically determines whether to insert a new vendor or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve vendors from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns vendor details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-Vnd"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_vendor tool. Using '@OPSS-Vnd' includes all fields from the OPSS-Vnd field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Vnd:Status",
          "OPSS-Vnd:Vendor Type",
          "OPSS-Vnd:Risk Rating",
          "OPSS-Vnd:Owner"
        ],
        "_comment": "USED BY: Type-based query_vendors tool ONLY"
      }
    },
    {
      "type_id": "Policy",
      "tool_prefix": "policy",
      "display_name": "Policy",
      "path_prefix": "Policies",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a policy in OpenPages. Automatically determines whether to insert a new policy or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve policies from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns policy details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-Pol"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_policy tool. Using '@OPSS-Pol' includes all fields from the OPSS-Pol field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Pol:Status",
          "OPSS-Pol:Policy Type",
          "OPSS-Pol:Review Date",
          "OPSS-Pol:Owner"
        ],
        "_comment": "USED BY: Type-based query_policies tool ONLY"
      }
    },
    {
      "type_id": "Resource",
      "tool_prefix": "asset",
      "display_name": "Asset",
      "path_prefix": "Resources",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update an asset (Resource) in OpenPages. Automatically determines whether to insert a new asset or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve assets (Resources) from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns asset details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-Res"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_asset tool. Note: Object type is 'Resource' but display name is 'Asset'. Using '@OPSS-Res' includes all fields from the OPSS-Res field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Res:Status",
          "OPSS-Res:Resource Type",
          "OPSS-Res:Criticality",
          "OPSS-Res:Owner"
        ],
        "_comment": "USED BY: Type-based query_assets tool ONLY"
      }
    },
    {
      "type_id": "RiskAssessment",
      "tool_prefix": "riskassessment",
      "display_name": "Risk Assessment",
      "path_prefix": "Risk Assessments",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a risk assessment in OpenPages. Automatically determines whether to insert a new risk assessment or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve risk assessments from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns risk assessment details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-RskAss"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_riskassessment tool. Using '@OPSS-RskAss' includes all fields from the OPSS-RskAss field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-RskAss:Status",
          "OPSS-RskAss:Assessment Type",
          "OPSS-RskAss:Overall Risk Rating",
          "OPSS-RskAss:Owner"
        ],
        "_comment": "USED BY: Type-based query_riskassessments tool ONLY"
      }
    },
    {
      "type_id": "RegChange",
      "tool_prefix": "regchange",
      "display_name": "Regulatory Change",
      "path_prefix": "Regulatory Changes",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a regulatory change in OpenPages. Automatically determines whether to insert a new regulatory change or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve regulatory changes from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns regulatory change details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-RegChg"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_regchange tool. Using '@OPSS-RegChg' includes all fields from the OPSS-RegChg field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-RegChg:Status",
          "OPSS-RegChg:Change Type",
          "OPSS-RegChg:Effective Date",
          "OPSS-RegChg:Owner"
        ],
        "_comment": "USED BY: Type-based query_regchanges tool ONLY"
      }
    },
    {
      "type_id": "Requirement",
      "tool_prefix": "requirement",
      "display_name": "Requirement",
      "path_prefix": "Requirements",
      "namespace": "openpages",
      "tool_descriptions": {
        "upsert": "Create or update a requirement in OpenPages. Automatically determines whether to insert a new requirement or update an existing one based on provided identifiers (id, path, or name).",
        "query": "Search and retrieve requirements from OpenPages. Supports filtering by name, owner, status, and custom field filters. Returns requirement details including ID, name, description, and selected fields."
      },
      "resource_fields": {
        "include_all_fields": true,
        "fields": [
          "@OPSS-Req"
        ],
        "_comment": "USED BY: Published resources (all modes) + Type-based upsert_requirement tool. Using '@OPSS-Req' includes all fields from the OPSS-Req field group"
      },
      "type_based_query_filters": {
        "fields": [
          "OPSS-Req:Status",
          "OPSS-Req:Requirement Type",
          "OPSS-Req:Compliance Status",
          "OPSS-Req:Owner"
        ],
        "_comment": "USED BY: Type-based query_requirements tool ONLY"
      }
    }
  ]
}
